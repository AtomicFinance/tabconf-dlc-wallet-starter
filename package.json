{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"yarn start --cwd frontend\" \"yarn start --cwd backend\"",
    "build": "yarn build --cwd frontend && yarn build --cwd backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
